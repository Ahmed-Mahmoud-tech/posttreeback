-- Create users table
CREATE TABLE IF NOT EXISTS users (
    -- ... your existing users table schema ...
);

-- Create posts table
CREATE TABLE IF NOT EXISTS posts (
    -- ... your existing posts table schema ...
);

-- ... rest of your schema ... 